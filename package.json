{
  "name": "console-brawl-api-serverless",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "dev": "serverless offline start --stage dev --skipCacheInvalidation",
    "deploy": "serverless deploy --stage prod -v",
    "deployV2": "serverless deploy --stage prod2 -v",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mitch Gavan",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.1",
    "mongoose": "^4.13.5",
    "serverless-offline": "^3.16.0"
  }
}
